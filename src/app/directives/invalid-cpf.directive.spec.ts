import { InvalidCpfDirective } from './invalid-cpf.directive';

describe('InvalidCpfDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidCpfDirective();
    expect(directive).toBeTruthy();
  });
});
